spring:
  application:
    name: demo-service
  cloud:
    config:
      uri: http://localhost:8888
    stream:
      kafka:
        binder:
          brokers: 192.168.1.222
  profiles:
    active: dev

management.endpoints.web.exposure.include: '*'
