server:
  port: 8080

spring:
  application:
    name: demo-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev

logging.level.org.springframework.cloud.config: debug
